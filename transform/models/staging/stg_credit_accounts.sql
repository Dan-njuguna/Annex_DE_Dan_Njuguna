SELECT
    loan_id,
    snapshot_date,
    customer_age,
    total_paid,
    total_due_today,
    balance,
    days_past_due,
    closing_balance,
    advance,
    arrears,
    balance_due_status,
    payment,
    expected_payment,
    account_status_l1,
    account_status_l2,
    weekly_rate,
    deposit,
    next_invoice_date,
    age,
    age_band,
    avg_monthly_income,
    avg_monthly_income_band,
    risk_category,
    ingested_at
FROM {{ source('public', 'cleaned_credit_data') }}
